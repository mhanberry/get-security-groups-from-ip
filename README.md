# Get Security Groups from pattern

Get a list of security groups whose configuration contains a pattern

## Dependencies

* aws-cli
* nodejs

## Usage

Execute the following command in a terminal:

```
./main.sh <profile> <pattern>
```
